#include <stdio.h>

enum DAY
{
      MON=1, TUE, WED, THU, FRI, SAT, SUN
};

int main(void)
{
	printf("MON = %d, TUE = %d, WED = %d, THU = %d, FRI = %d, SAT = %d, SUM = %d\n", MON, TUE, WED, THU, FRI, SAT, SUN); 
	return 0;
}
